/**
 * OneDua
 *
 * @author Afaan Bilal
 * @link   https://afaan.dev
 */

export type WithChildren = {
    children: React.ReactNode;
};

export type Dua = {
    id: string;
    text: string;
    audio?: string;
};
